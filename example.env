POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

APP_PORT=5005

# WB API (https://common-api.wildberries.ru)
WB_API_TOKEN=

# URL тарифов коробов (по умолчанию: https://common-api.wildberries.ru/api/v1/tariffs/box)
# WB_BOX_TARIFTS_URL=

# Google Sheets: path to service account JSON file, or leave empty and set GOOGLE_SERVICE_ACCOUNT_JSON to JSON string
GOOGLE_CREDENTIALS_PATH=credentials/credentials.json
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Intervals in milliseconds (for testing use lower values, e.g. SHEETS_SYNC_INTERVAL_MS=60000)
TARIFS_FETCH_INTERVAL_MS=3600000
SHEETS_SYNC_INTERVAL_MS=1800000

# Google spreadsheet IDs for tariffs export (comma-separated). Sheet name: stocks_coefs.
SPREADSHEET_IDS=id1,id2,id3